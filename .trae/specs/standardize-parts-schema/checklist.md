- [x] تحليل شامل لهيكل `Part` في قاعدة البيانات وتحديد التعديلات المطلوبة.
- [x] تعديل الواجهة الخلفية وقاعدة البيانات (Migration & Model) لتشمل الحقول: `oem_number`, `part_name`, `supplier_code`, `brand_origin`, `price_cash`, `price_bank`.
- [x] توحيد مسميات الأعمدة في خدمة البايثون (Column Aliasing) لتتطابق مع الحقول القياسية (مثل "اسم الصنف", "البيان", "المادة" -> `part_name`).
- [x] تطبيق التعبيرات النمطية (Regex) في خدمة البايثون لاستخراج `oem_number` من حقل "البيان/اسم الصنف" عند عدم وجوده في عمود منفصل.
- [x] تعديل الواجهة الأمامية (`FileDetails.vue` أو المكون المسؤول) لعرض الأعمدة الـ 6 القياسية فقط وتجاهل باقي الأعمدة غير المرغوبة.
- [x] رفع ملف PDF وتأكيد أن استخراج وعرض البيانات يعمل بشكل صحيح ونظيف دون تشوه.