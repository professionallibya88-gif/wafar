- [x] نموذج `Admin` ومستودع `AdminRepository` تم إنشاؤهما بشكل صحيح وتم عمل التهجير (Migration) بنجاح.
- [x] دور `admin` تمت إزالته نهائياً من نموذج `User`.
- [x] تم إنشاء مسارات المصادقة المستقلة للمديرين (`/api/admin/auth/login`).
- [x] الميدلوير (`middleware/auth.ts`) يفرق بشكل صحيح ويمنع الدخول المتبادل (المدير لا يمكنه دخول مسارات المستخدم والعكس).
- [x] توجيهات الواجهة الأمامية (Router) تم تحديثها لمنع دخول المدير كمستخدم عادي وفصل جلسات تسجيل الدخول.
- [x] لوحة التحكم تسجل الدخول بنجاح باستخدام مسارات المديرين الجديدة وصفحة الدخول الخاصة بها.
- [x] قسم "إدارة المستخدمين" في لوحة التحكم لم يعد يعرض أي خيارات لإنشاء أو إدارة المديرين.
- [x] تم إضافة قسم جديد "إدارة المديرين" في لوحة التحكم يعمل بشكل كامل (عرض، إضافة، تعديل).
