- [x] توافق جميع الحقول والجداول في نماذج قاعدة البيانات مع صيغة `snake_case`.
- [x] لا يوجد أي استدعاء مباشر لـ Models خارج مجلد `repositories`.
- [x] جميع المتحكمات تستخدم `asyncHandler` وتقوم بإرجاع الاستجابات باستخدام `ApiResponse`.
- [x] لا توجد أكواد مكررة أو دوال غير مستخدمة في الـ Backend.
- [x] الواجهة الأمامية خالية من الاستيرادات غير المستخدمة وتلتزم بالمعايير المعمارية (استخدام `services/storage.js`).
- [x] تسميات المتغيرات والدوال موحدة (camelCase في JS/TS) وواضحة.
- [x] خدمة البايثون تعمل باستقرار وتم تنظيف الكود الخاص بها.
- [x] يمر فحص الجذر (`lint`/`build` على مستوى المشروع) بدون أخطاء.
- [x] يمر فحص Backend (`lint` و`build`) بدون أخطاء.
- [x] يمر فحص Frontend (`lint` و`build`) بدون أخطاء.
- [x] يمر فحص Python Service (compile/tests) بدون أخطاء.
- [x] لا توجد أخطاء متبقية معروفة بعد إعادة التحقق الشامل.
