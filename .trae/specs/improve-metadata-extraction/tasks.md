# المهام

- [x] Task 1: تحسين خوارزمية الاستخراج في الخلفية
  - [x] SubTask 1.1: تعديل الأنماط (Regex) في `extractSupplierName` بملف `pdfProcessor.ts` للتعرف بشكل أفضل على ترويسة الشركات مثل "شركة القمة الدولية" وتجاهل ترويسات الجداول (نقدي، مصرف، سعر).
  - [x] SubTask 1.2: إضافة دعم أنماط التاريخ الجديدة مثل "تاريخ الطباعة : DD/MM/YYYY" في `extractDocumentDate` بملف `pdfProcessor.ts`.
  - [x] SubTask 1.3: إزالة كود إنشاء "مورد تلقائي UUID" من دالة `processPDF` في `pdfProcessor.ts` والاعتماد الكلي على قيمة `supplierId` المستلمة من الطلب.

- [x] Task 2: جعل حقول الواجهة الأمامية إجبارية (UploadPDFView.vue)
  - [x] SubTask 2.1: إزالة نص "(اختياري)" من حقلي الشركة الموردة وتاريخ الملف.
  - [x] SubTask 2.2: إضافة خاصية "مطلوب" أو تعطيل زر الرفع إذا كان `supplierId` أو `documentDate` فارغاً.
  - [x] SubTask 2.3: السماح للمستخدم بتعديل أو إدخال هذه الحقول يدوياً بكل سهولة بعد محاولة النظام استخراجها تلقائياً.

- [x] Task 3: تحديث مسارات المصادقة بالخلفية
  - [x] SubTask 3.1: تعديل قواعد التحقق في `pdfValidator.ts` لجعل `supplier_id` و `document_date` حقولاً إلزامية (Required).
  - [x] SubTask 3.2: تعديل `uploadPDF` بملف `PDFService.ts` للتأكد من وجود `supplier_id` و `document_date` قبل المعالجة وإرجاع خطأ في حال نقصانهما.
  - [x] SubTask 3.3: تحديث الاختبارات (Tests) ذات الصلة لضمان نجاح رفع الملفات بالحقول الجديدة الإجبارية.
