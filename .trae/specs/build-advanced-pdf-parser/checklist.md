- [x] تم تثبيت مكتبتي `PyMuPDF` (`fitz`) و `pandas` في بيئة خدمة الـ Python بنجاح.
- [x] تمت ترقية `TableExtractor` لاستخدام `PyMuPDF` لاستخراج الجداول من ملفات الـ PDF النصية (Digital PDFs).
- [x] يتم دمج جداول الصفحات المتعددة (100+ صفحة) بكفاءة عالية في جدول واحد نظيف عبر `pandas`.
- [x] تمت معالجة رؤوس الجداول المتكررة وحذفها بنجاح لضمان عدم وجود صفوف "اسم الصنف" و "السعر" كبيانات.
- [x] الكلمات العربية تُستخرج بشكل سليم وغير معكوس أو مشفر وبدون رموز غريبة مثل `(cid:X)`.
- [x] عملية الإدراج للحفظ في الخلفية تستخدم `bulkCreate` مع `updateOnDuplicate` (أو `ON CONFLICT DO UPDATE`).
- [x] تم تنفيذ اختبار للتحقق من استخراج ملف `C:\files-2.pdf` في غضون ثوانٍ.