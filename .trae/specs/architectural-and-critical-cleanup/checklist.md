- [x] خلو الواجهة الخلفية من أي استعلامات مباشرة لقاعدة البيانات خارج طبقة Repositories.
- [x] خلو المتحكمات (Controllers) من `try/catch` واعتمادها التام على `asyncHandler`.
- [x] جميع الأخطاء المرمية من الخدمات (Services) تورث من `AppError` (مثل `NotFoundError`, `ValidationError`).
- [x] الواجهة الأمامية لا تستخدم `localStorage` مباشرة في متاجر Pinia وتعتمد على `services/storage.js`.
- [x] اجتياز `npm run lint` لكامل المشروع بنجاح وبدون أخطاء.
- [x] اجتياز كافة الاختبارات `npm run test` بنجاح.
- [x] عدم وجود أكواد ميتة أو ملفات غير مستخدمة تؤثر على صيانة المشروع.
