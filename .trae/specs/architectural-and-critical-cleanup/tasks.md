# المهام

- [x] Task 1: فحص وتحليل النظام واكتشاف الأخطاء
  - [x] SubTask 1.1: تشغيل أدوات الفحص (Linting) واختبارات الوحدة (Jest/Vitest) وتسجيل جميع الأخطاء.
  - [x] SubTask 1.2: البحث عن التجاوزات المعمارية (استخدام Sequelize خارج Repositories، أو try/catch في Controllers).
- [x] Task 2: إصلاح أخطاء الواجهة الخلفية (Backend)
  - [x] SubTask 2.1: نقل أي استعلامات قاعدة بيانات مباشرة إلى المستودعات (Repositories).
  - [x] SubTask 2.2: إزالة منطق الأعمال من المتحكمات (Controllers) ونقله إلى الخدمات (Services).
  - [x] SubTask 2.3: توحيد نظام الاستجابات وإدارة الأخطاء ليعتمد بالكامل على `AppError` و `ApiResponse`.
- [x] Task 3: إصلاح أخطاء الواجهة الأمامية (Frontend)
  - [x] SubTask 3.1: التأكد من أن جميع متاجر Pinia تستخدم `storage.js` بدلاً من `localStorage` مباشرة.
  - [x] SubTask 3.2: تنظيف الأكواد غير المستخدمة وتوحيد طبقات العرض (z-index) للواجهات العائمة.
  - [x] SubTask 3.3: التأكد من توجيه كافة نداءات API عبر `services/http.js` بشكل صحيح ومعالجة أخطاء 401.
- [x] Task 4: التحقق النهائي والتنظيف
  - [x] SubTask 4.1: تشغيل `npm run lint` وتصحيح كافة التحذيرات المتبقية.
  - [x] SubTask 4.2: تشغيل كافة الاختبارات للتأكد من عدم تعطل أي ميزة رئيسية بسبب التعديلات المعمارية.
