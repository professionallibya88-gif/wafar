- [x] إزالة ملفي `adminHttp.js` و `adminAuth.js` من الواجهة الأمامية تماماً.
- [x] التأكد من أن `http.js` هو العميل الوحيد المستخدم في كافة ملفات `services/api`.
- [x] نجاح تسجيل دخول المستخدم العادي وبقاء الجلسة صالحة.
- [x] نجاح تسجيل دخول المدير من `/admin/login` وبقاء الجلسة صالحة كمدير.
- [x] قدرة المدير (المسجل دخوله) على زيارة صفحة `/upload` دون أن يتم إعادة توجيهه إلى صفحة تسجيل الدخول.
- [x] قدرة المدير على إجراء عمليات الرفع والتفاعل في مسارات المستخدمين دون التسبب بأخطاء 401 أو Foreign Key في الواجهة الخلفية.
- [x] التوجيه بعد فشل المصادقة (401) يعيد توجيه المدير إلى `/admin/login` والمستخدم إلى `/login` بناءً على السياق، دون الدخول في حلقة إعادة توجيه (Infinite Redirect Loop).