# المهام (Tasks)

- [x] Task 1: فحص هيكل الواجهة الخلفية (Backend)
  - [x] SubTask 1.1: مراجعة مجلد `repositories` والتأكد من تمركز استعلامات قواعد البيانات فيه.
  - [x] SubTask 1.2: مراجعة مجلد `services` للتأكد من خلوه من الاستعلامات المباشرة واعتماده على المستودعات.
  - [x] SubTask 1.3: استكشاف هيكلية الموجهات (Routes) والمتحكمات (Controllers).
- [x] Task 2: فحص الواجهة الأمامية (Frontend)
  - [x] SubTask 2.1: مراجعة بنية المجلدات `components` و `stores` و `services/api`.
  - [x] SubTask 2.2: التحقق من مركزية التخزين في `storage.js` وعدم استخدام `localStorage` مباشرة في المتاجر.
- [x] Task 3: فحص مخطط قاعدة البيانات (Database Schema)
  - [x] SubTask 3.1: فحص النماذج (Models) في `apps/backend/src/database/models` لفهم العلاقات (Associations).
  - [x] SubTask 3.2: التعرف على هيكلية جداول النظام (المستخدمين، الموردين، الطلبات، سلة المشتريات، إلخ).
- [x] Task 4: تقديم التقرير النهائي
  - [x] SubTask 4.1: إعداد ملخص شامل للمعمارية والمخطط وإرساله للمستخدم.
