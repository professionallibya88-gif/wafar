- [x] المكون `BasePagination` موجود ومصمم بشكل احترافي وموحد.
- [x] المكون يدعم عرض أرقام الصفحات مع اختصار الصفحات البعيدة (`...`).
- [x] المكون يحتوي على قائمة اختيار عدد الأسطر (20, 50, 100, 200).
- [x] المكون يحتوي على حقل انتقال مباشر لصفحة معينة (Jump to page).
- [x] شريط الترقيم يظهر أعلى وأسفل الجداول في جميع الصفحات المستهدفة.
- [x] عند تغيير عدد الأسطر في الصفحة، يتم جلب البيانات بالعدد الجديد والعودة للصفحة 1 تلقائياً.
- [x] الانتقال المباشر لصفحة معينة يعمل بشكل صحيح ويتحقق من الحدود (لا يقبل أرقاماً خارج النطاق).
- [x] تم استبدال جميع أزرار الترقيم القديمة في لوحة التحكم الإدارية (Admin Views) بالمكون الجديد الموحد.
- [x] تم استبدال جميع أزرار الترقيم القديمة في لوحة تحكم المستخدم (Dashboard Views) بالمكون الجديد الموحد.
- [x] تم فحص النظام بالكامل والتأكد من خلوه من أخطاء الـ Lint أو أخطاء برمجية في الكونسول.