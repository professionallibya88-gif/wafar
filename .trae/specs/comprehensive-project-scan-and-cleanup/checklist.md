- [x] خلو متحكمات الواجهة الخلفية (Controllers) من أي استدعاءات مباشرة لنماذج قاعدة البيانات (Models).
- [x] التزام الواجهة الخلفية (Backend) برميات الأخطاء المخصصة (AppError) واستخدام `asyncHandler` و `ApiResponse`.
- [x] استخدام `services/storage.js` حصراً للتعامل مع التخزين المحلي في الواجهة الأمامية (Frontend).
- [x] عدم وجود أي إيموجيز في واجهة المستخدم، واستخدام الأيقونات عبر المكون `AppIcon.vue` فقط.
- [x] نجاح الفحص `npm run lint` للواجهة الخلفية دون أخطاء.
- [x] نجاح الفحص `npm run lint` للواجهة الأمامية دون أخطاء.
- [x] تنظيف جذر المشروع من الملفات والسكربتات المؤقتة غير الضرورية.
- [x] عدم وجود أي استيرادات غير مستخدمة (Unused Imports) في المشروع.